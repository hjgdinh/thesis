const monday = document.querySelectorAll();
const tuesday = document.querySelectorAll();
const wednesday = document.querySelectorAll();
const thursday = document.querySelectorAll();
const friday = document.querySelectorAll();
const saturday = document.querySelectorAll();
const sunday = document.querySelectorAll();
//
const day = document.querySelectorAll(".day");
function handleClickButton() {
  if (condition) {
  }
}
